
constant VEL 95
variable MAX 60

functions booga 999

void boing(int notenum = 0)
{
    //println("boing");
    if(notenum == 0)
    {
        notenum = (int)random(30, MAX);
    }
    sendMidiNote(SYNTH, notenum, VEL, 1.09);
}
