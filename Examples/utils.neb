
// Define some Neb vars.
const double VEL = 0.8;

Random rand = new();

///// Functions /////

// int notavar = 911;

public bool Boing(int notenum = 0)
{
    bool boinged = false;

    // Print("boing");
    if (notenum == 0)
    {
        notenum = rand.Next(30, 80);
        boinged = true;
    }

    //SendNote("synth", notenum, VEL, 1.0);
    return boinged;
}
