
VEL = const(95);
MAX = var(60);

void boing(int notenum = 0)
{
    //println("boing");
    if(notenum == 0)
    {
        notenum = (int)random(30, MAX);
    }
    sendMidiNote(BASS, notenum, VEL, 1.09);
}
